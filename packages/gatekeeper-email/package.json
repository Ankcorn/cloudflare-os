{
  "name": "@gadgets/email-gatekeeper",
  "version": "1.0.0",
  "type": "module",
  "main": "./src/email.ts",
  "scripts": {
    "dev": "echo \"run 'pnpm dev-server' in the root directory instead\" >&2 && exit 1",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@gadgets/workshop-shared": "workspace:*",
    "capnweb": "^0.6.1",
    "postal-mime": "^2.7.3"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.71.0"
  }
}
