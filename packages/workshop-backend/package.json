{
  "name": "@minions/workshop-backend",
  "version": "1.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "echo \"run 'pnpm dev-server' in the root directory instead\" >&2 && exit 1",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@minions/typed-storage": "workspace:*",
    "@minions/workshop-shared": "workspace:*",
    "capnweb": "^0.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251111.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.47.0"
  }
}