{
  "name": "@gadgets/google-gatekeeper",
  "version": "1.0.0",
  "type": "module",
  "main": "./src/google.ts",
  "scripts": {
    "dev": "echo \"run 'pnpm dev-server' in the root directory instead\" >&2 && exit 1",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@gadgets/workshop-shared": "workspace:*",
    "capnweb": "^0.4.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.63.0"
  }
}