{
  "name": "@minions/google-gatekeeper",
  "version": "1.0.0",
  "type": "module",
  "main": "./src/google.ts",
  "scripts": {
    "dev": "echo \"run 'pnpm dev-server' in the root directory instead\" >&2 && exit 1",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@cloudflare/jsrpc": "file:../../../jsrpc",
    "@minions/workshop-shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250813.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.32.0"
  }
}