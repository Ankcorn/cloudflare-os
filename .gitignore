# Dependencies
node_modules/

# Build outputs
dist/
packages/gatekeeper-*/src/generated/

# TypeScript build info
*.tsbuildinfo

# Cloudflare Workers
.wrangler/
.env

# Local dev secrets (loaded by run-dev-server.js)
.dev.vars*

# Site-specific production deployment configs.
wrangler.prod.jsonc

# Site-specific dev server configs.
wrangler.dev.jsonc

# macOS
.DS_Store

# Wrangler local dev secrets (never commit). Matches root and per-package .dev.vars files.
.dev.vars
.dev.vars.*
**/.dev.vars
**/.dev.vars.*
